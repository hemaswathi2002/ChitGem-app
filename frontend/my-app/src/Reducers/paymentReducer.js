const chitPament = {
    chitPament:[],
}

const paymentReducer = (state=chitPament , action)=>{
    switch(action.type){
        
        default:{
            return {...state}
        }
    }
}

export default paymentReducer 
    ;